# Emacs Configuration 

- this is my "literate" emacs config
- don't forget the init.el which must contain

```lisp
(org-babel-load-file
  (expand-file-name
    "config.org"
    user-emacs-directory))
```
